# Local overlay — WSL2 / Docker Desktop with GPU via /dev/dxg
# No GPU resource requests needed; no StorageClass override.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base
